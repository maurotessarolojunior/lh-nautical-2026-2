Com base na análise exploratória, a tabela `orders` possui 48.998 linhas e cobre o período de 2020-01-01 a 2026-12-31.

- **Possíveis outliers em `total`:** os valores variam de R$ 32,62 a R$ 127.262,02, com média de R$ 28.704,99. O valor máximo deve ser investigado em conjunto com `order_items` e os preços unitários antes de ser tratado como erro ou removido.

- **Qualidade dos dados:** não há nulos nas colunas essenciais (`id`, `customer_id`, `total` e datas), duplicidades em `id` ou `order_number`, nem valores de `total` nulos, iguais a zero ou negativos. A relação `subtotal - discount_amount = total` também é válida em todas as linhas. Há 24.131 valores nulos em `salesperson_id`, por isso essa coluna exige cuidado nas análises por vendedor.

- **Conclusão:** a tabela está adequada para as agregações simples desta questão. Para análises de negócio, deve ser relacionada às demais tabelas, principalmente `order_items`. Também é necessário confirmar com a área de negócio se pedidos com status `cancelled` ou `draft` devem participar das métricas de faturamento.
