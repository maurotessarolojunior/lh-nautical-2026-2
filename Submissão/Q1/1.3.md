Com base na análise exploratória da tabela `orders` (48.998 linhas, período de 2020-01-01 a 2026-12-31):

- **Possíveis outliers em "total":** os valores vão de R$ 32,62 a R$ 127.262,02, com média de R$ 28.704,99 — o máximo fica bem acima da média, o que sugere uma cauda de valores altos. No contexto de varejo náutico (motores de popa, embarcações), pedidos de dezenas de milhares de reais são plausíveis e não parecem erro de digitação — mas isso não é uma confirmação: só relacionando com `order_items` e preços unitários dá pra validar se são pedidos legítimos de grande porte.

- **Qualidade dos dados:** não há nulos em nenhuma coluna essencial (`id`, `customer_id`, `total`, datas), nem duplicidade em `id`/`order_number`, nem `total` nulo, zero ou negativo, e `subtotal - discount_amount` bate com `total` em 100% das linhas. O único nulo relevante é `salesperson_id` (24.131 linhas), concentrado no canal `ecommerce` — mas não é uma regra fechada: 10.211 dos 34.342 pedidos `ecommerce` (~30%) têm vendedor preenchido, então trato como um padrão a investigar, não como explicação estrutural. A base é fictícia e cobre 2020–2026 (inclusive datas futuras em relação a hoje), coerente com o que o enunciado descreve.

- **A tabela está pronta para análise?** Para as agregações simples pedidas aqui, sim. Para as perguntas de negócio das próximas questões, ainda não isoladamente: falta confirmar a consistência das demais tabelas (em especial `order_items`, só verificável após a carga completa do schema), e a inclusão de pedidos `cancelled`/`draft` em métricas de faturamento precisaria ser confirmada com o negócio antes de decidir um filtro.
